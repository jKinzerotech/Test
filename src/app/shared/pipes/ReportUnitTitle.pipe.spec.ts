

import { TestBed, } from '@angular/core/testing';
import { ReportUnitTitlePipe } from './ReportUnitTitle.pipe';

describe('Pipe: ReportUnitTitlee', () => {
  it('create an instance', () => {
    const pipe = new ReportUnitTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
