

import { TestBed } from '@angular/core/testing';
import { PointFormatTypePipe } from './PointFormatType.pipe';

describe('Pipe: PointFormatTypee', () => {
  it('create an instance', () => {
    const pipe = new PointFormatTypePipe();
    expect(pipe).toBeTruthy();
  });
});
